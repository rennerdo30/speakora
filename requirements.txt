torch >= 2.0.0
torchaudio >= 2.0.0
transformers >= 4.35.0
protobuf >= 4.21.0
tiktoken >= 0.5.0
sentencepiece >= 0.1.99
soundfile >= 0.12.0
librosa >= 0.10.0
pydantic >= 2.0.0
pyyaml >= 6.0
python-dotenv >= 1.0
rich >= 13.0.0
click >= 8.0.0
sqlalchemy >= 2.0.0
fastapi >= 0.104.0
uvicorn >= 0.24.0
websockets >= 12.0.0
psutil >= 5.9.0

# Optional: silero-vad for advanced VAD (not required - simple RMS-based VAD is used by default)
# silero-vad >= 4.0.0  # Requires onnxruntime which may not be available on all platforms
